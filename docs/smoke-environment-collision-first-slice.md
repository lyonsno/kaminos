# Smoke–environment collision: first implementation slice

Status: provisional implementation brief for operator inspection, not an implemented capability or Luna launch. Source inspected: `origin/main@4b4c02c72fb3452654d661504cf09d2a6413630c` (refetched 2026-09-26). Owner: `sexy-fireman`; intended implementer: Luna; consumer: the live Kaminos volume scene and its authored environment. The object is smoke and hot gas *moving around* a scene solid while retaining the newly landed plume behavior, not a depth mask that merely hides smoke inside geometry. The first live environment object and solver-compatibility route remain operator/composition decisions; the closed box below is a numerical control, not the product acceptance target.

## Current seam

`volume-core.js` holds the `N × 2N × N` fluid/front/pressure fields. `encodeSim` runs transport, bounded analytic emitter injection, and then pressure projection. The opt-in converged solver uses upper-face velocity, backward divergence, a compact pressure operator, and forward-gradient projection. It is a natural numerical seam for an obstacle but **not** an automatically lawful replacement for the current basin's effective dynamics. The older saved blue-basin replay route in `/Users/noahlyons/.local/state/kaminos/evidence/smoke-volume-integration-0924/blue-basin-native-011/witness-report.json` omits `volume_pressure_solver` and `volume_advection_scheme`, and requests `volume_projection=1.5`; on this main revision the omitted solver resolves to `legacy`. That receipt is a nearby-settings replay of the earlier blue basin, not an identity for Sexy Flame Doctor's newer moving tongues. Capture the actual candidate basin's effective solver, transport and projection before choosing collision numerics or claiming motion preservation.

`index.html` owns registered `sceneObjects` and their Three.js world transforms. It passes `getSceneDepth` to the volume prototype only for compositing/ordinary-scene visibility. Neither that depth provider nor a visible mesh is currently a volume-flow obstacle. No scene-object geometry or occupancy field reaches the solver on this main revision. The annular burner is visual scene geometry under an emitter; it is not automatically a collider and must not swallow the inlet.

## First numerical control and live-consumer target

1. Use a **closed box** as a small deterministic solver control only. The first live acceptance target must be a named authored environmental solid the operator actually wants smoke to encounter, with stable scene-object identity, exact visible geometry/transform, and enabled state. Choose that object before committing to a voxelizer or GPU representation. Do not treat every imported GLB/OBJ, splat, image plane, floor, or depth surface as a collider. Do not call an arbitrary mesh's bounding box a mesh collision.
2. Convert the selected authored shape through the inverse product/volume transform into volume-local space, then construct a deterministic solid-cell field and blocked-face topology at the current grid resolution. A static transform/config change rebuilds once, not every frame. Record source ID, geometry/transform revision, grid, requested/effective collision mode, solid-cell count, blocked-face count, and rebuild step. An invalid transform, unsupported shape, or source–solid overlap reports a specific inactive/error state rather than quietly reverting to visual-only depth. Preserve the exact collision-off path and saved basins.
3. Apply one impermeable-wall law coherently across the simulation:
   - No advected gas/scalar/front sample may tunnel through a blocked face; a backtrace reaching solid terminates at the fluid side. Interpolation and MacCormack extrema must not import solid-cell values. Fully solid cells carry no transported smoke, fuel, heat, front, or velocity.
   - The upper-face velocity is zero where either adjacent cell is solid (static wall); compact divergence reads that same masked flux. The pressure update includes only open fluid neighbors in its sum and diagonal, with a well-defined isolated-fluid-cell case. Projection subtracts gradients only on open faces and leaves blocked faces zero. The residual probe reports fluid-cell denominator and boundary flux separately, so a low aggregate residual cannot hide a leak.
   - Emitter injection into solid is rejected/blocked with an overlap diagnostic. Do not clip the final raymarch as a substitute for transport or pressure collision. Preserve the existing open-top outlet where it is not occluded by a solid.
4. Do **not** force the converged/full-projection path into an operator basin as an incidental collision prerequisite. If the chosen basin uses legacy or partial projection, first establish a matched solver-only comparison and decide whether the new law retains the valued motion; otherwise design collision under the effective legacy law with a truthful no-through claim ceiling. A solver/collision combination that cannot prevent crossing must fail loudly or report collision **inactive** with the exact reason. Audit WebGPU per-stage storage-buffer limits and bind-group layouts before adding a field buffer; do not assume one more binding fits every pipeline.

The numerical box should sit above, not within, a source and intersect the upward plume, but it is not the product witness. The first live witness uses the selected authored environment object, with the same visible and simulated geometry identity. Keep the solver interface field-shaped so later watertight meshes can supply it without pretending a synthetic box or bounding box proves mesh collision.

## Acceptance and evidence

- Fail-first deterministic tests: collision off preserves an existing step/state contract; a box blocking a channel gives zero normal face flux and no transported material inside; pressure/divergence/residual use identical open-face topology; a fluid-side backtrace and MacCormack stencil cannot sample through the box; source overlap and unsupported solver fail explicitly; grid/transform revisions trigger one rebuild and cannot silently reuse stale occupancy.
- Effective-route evidence: a native WebGPU run records the named source/preset, effective solver/transport/collision modes, authored solid source ID and transform revision, grid, step range, complete frame readback, and pressure/boundary diagnostics. No default/fallback route may impersonate it. Preserve frames, not just a pass/fail summary.
- Visual comparison: inspect a short sequence at matched source, camera, solver, transport, and evolved-state range with the chosen authored collider off/on. The plume should deflect around the actual environment object and form a downstream wake; material must not pass through or simply vanish at it; the moving tongues outside the obstacle should remain recognizable. If the solver mode changes, first capture that as a separate A/B. A static screenshot or a low divergence number alone cannot close this visual claim.
- Cost: report occupancy rebuild time, added GPU resident bytes, and steady-frame cost on the actual route. A static environment object should not require per-frame CPU voxelization or a new full-grid readback.

## Mutation boundary and next handoff

After the operator names the first environment object and its basin, Luna owns the opt-in descriptor/scene adapter, field construction, solver/transport changes, fail-first box control, and native environment witness on a Kaminos feature worktree. `sexy-fireman` owns composition with that basin, review, source-truth verdict, and eventual main integration only after operator authorization. Handy's light/raymarch work and Sexy Flame Doctor's source/confinement/time-step behavior are adjacent and should not be rewritten to make the collision demo pass. The unlanded `cc/flame-doctor-time-step-0926@ad521770` adds an opt-in uniform time-step mode and emitter/transport coupling; it is not current main or an implicit precondition for collision work. Re-evaluate integration if it lands before Luna starts.
