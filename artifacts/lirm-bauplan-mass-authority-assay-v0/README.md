# LIRM Bauplan Mass-Authority Assay V0

This matched assay asks whether a deterministic low-frequency bauplan edit can
survive neutral image completion and spatial reconstruction without placing
mass words in the generator prompt.

## Result

The result is positive within a narrow claim boundary.

- The procedural heavy control is visibly fuller than its accepted parent.
- Fixed-seed neutral image completion preserves that mass difference while
  keeping both branches in the same abstract organismal basin.
- Fixed-seed TRELLIS reconstruction preserves the difference in 3D. The parent
  remains long and low; the heavy branch becomes shorter, deeper, and fuller.
- The route establishes low-frequency morphology authority. Exact dorsal-detail
  and support-topology authority remain unestablished.
- Both outputs were inspected and classified `happy_safe`. Neither is admitted
  as a finished hero creature.

The neutral completion prompt requested a four-support pattern while the
procedural bauplan carries seven authored axial contacts. The assay therefore
admits no support-count or support-identity claim from the generated images or
GLBs. Mushfinger's registration layer remains the named consumer for that
question.

## Evidence

- `control-sheet.png`: parent and heavy clay, depth, and normal controls.
- `imagegen/matched-seed720501/comparison-parent-left-heavy-right.png`: matched
  neutral image outputs.
- `spatial/matched-seed720501/comparison/parent-left-heavy-right-front-side.png`:
  matched front-quarter and side spatial witnesses.
- `spatial/matched-seed720501/comparison/parent-left-heavy-right-rear-opposite.png`:
  matched rear-quarter and opposite-side spatial witnesses.
- `spatial/matched-seed720501/glbs/`: semantically named parent and heavy GLB
  links for direct consumer pickup.
- `assay-result.json`: route identities, job ids, hashes, rejected submissions,
  claim boundaries, and the next consumer question.

The committed TRELLIS per-job metadata records seed, steps, target face count,
and texture size. Resolution, cascade state, and simplify-first state came from
the submitted Greenroom request and are labeled separately in the receipt.

## Interpretation

Gross mass can live in the bauplan and survive both generative transitions.
Imagegen remains useful for organismal completion and aesthetic treatment.
Support identity still needs an explicit registration/admission layer because
the generators may merge, split, or suppress visible support structures.
