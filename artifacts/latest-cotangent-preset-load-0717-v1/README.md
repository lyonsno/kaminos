# Latest Cotangent Preset Load Witness

This artifact preserves the operator-reported failure path after its repair. The browser starts on the generic ordinary-volume route used by the prior direct-union witness, selects `big_raymarch_hero_flamebowl_cotangent_covariance`, leaves the invocation selector on `Current renderer view`, and invokes the real `Open Fresh` command.

## Identity

- Requested and effective preset: `vsp-902ef7efc65170f032f01f9dfff7b8c776b64a01e6c80caf2c4ec97004b461e7`
- Preset authority: `shared-volume-settings-preset-v2`
- Resolved invocation: `smoke-hybrid`
- Requested/effective composition: `smoke-raymarch-under-splats-v0`
- Backend: `WebGPU:apple`

## Result

The ordinary viewer's requested/effective composition now resolves the `Current renderer view` selector without requiring a selective-head wrapper. The reopened target reports both raymarch and splat passes encoded and applied, with no preset, composition, or boundary-splat fallback. The preset status computed style is `white-space: normal`, `overflow: visible`, `text-overflow: clip`, `overflow-wrap: anywhere`, and `user-select: text`, so a future failure remains readable instead of inheriting the compact readout ellipsis.

The screenshot was inspected after capture. It is nonblack, shows the large Flamebowl fire body, preserves the left volume controls and right Authored Mix panel, and visibly wraps the long preset-store receipt across lines. This witness establishes loader/runtime plumbing and rendered-pixel presence; it does not claim visual parity or physical correctness.

## Files

- `report.json`: requested route, exact UI command result, effective route, preset identity, requested/effective renderer state, pass receipt, and diagnostic computed style.
- `latest-cotangent-preset-smoke-hybrid.png`: inspected `1440x813` browser frame, SHA-256 `55daa2c5519f03d9b24e18fead8400767e5af185d862c93daf361a138997d81e`.
