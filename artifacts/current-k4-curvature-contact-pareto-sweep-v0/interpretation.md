# Current-K4 curvature contact Pareto interpretation

The six caller-owned candidates form a real residual-versus-centerline-shape frontier; the sweep applied no candidate cap and no config fallback. Lower regularization buys more pairwise and skeletal contact relief by allowing more distributed centerline bending. Higher regularization retains straighter construction character while conceding residual contact.

The verified three-quarter and side contact sheets agree with those measurements. Curvature `1.5` and `3` visibly fan the red and cyan constructions hardest. Curvature `24` and `48` keep the cluster straighter and more crowded. Curvature `6` and `12` are the useful transition band, and neither recreates the prior hard-elbow failure.

`curvature-12` is the selected provisional mechanism. It is the closest candidate to the independently normalized two-objective ideal (`0.40194258216889955`, versus `0.4226160895048092` for `curvature-6`), retains smooth attachment-to-attachment arcs in both inspected views, and materially opens the source-crowded cluster. Its remaining contact residual is binding evidence, not a hidden completion claim.

Claim ceiling: cage-contact mechanism selection only. This result does not establish authored anatomical correctness, operator visual admission, full compartment packing, or production admission.
