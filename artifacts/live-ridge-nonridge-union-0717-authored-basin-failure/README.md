# Authored Basin Source-Admission Failure

This retained failure is not a visual witness. The exact authored Flamebowl query admitted the native WebGPU renderer, but both a 2-second and a 15-second settle produced zero union candidates at the frozen capture boundary. The harness failed at `boundary-splat-footprint-audit-blank-candidate-population` before accepting an image.

`report.json` is the durable 15-second failure receipt. It rules out route/backend admission failure, but it does not establish why this direct-query source state had no admissible cells. The passing live union witness is in `../live-ridge-nonridge-union-0717-v1/`.
