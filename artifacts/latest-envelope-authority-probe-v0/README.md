# Latest-envelope authority probe

This two-cell assay holds the FLUX route and seed fixed while replacing the prior revision-025 source with the operator-authored revision-029 envelope. The mannequin cell asks whether better anatomical organization shifts the known seed-80413 fox/wolf basin toward the source's increasingly feline morphology. The coat cell separately asks whether concise representation-level language can produce broad, separated fur masses suitable for later TRELLIS inspection.

The source plate is rendered by the same orthographic Blender Workbench recipe as the predecessor. `campaign.json` freezes the source hashes, exact prompts, settings, comparison predecessor, and claim ceiling. The result ledger and adjacent operator sheet are written only after both Greenroom jobs return and their effective routes are authenticated.
