# MLX malformed-coat carrier recovery v0

This exact-route, exact-seed assay asks whether the topology signal admitted by `fur-signal-metric-atlas-v0` contains enough spatial information to reconstruct a coherent carrier shell and place one static replacement groom.

The answer is split:

- **Clean carrier recovery is admitted for short-fur seed 80301.** CPU solidification plus voxel fusion at `0.026` of the selected geometry diagonal converts the malformed exterior into one connected, visibly cleaner carrier while retaining at least `0.9691` projected silhouette IoU and `0.9792` axis-bound retention.
- **Static groom placement is admitted as a mechanism witness.** Deterministic crossed-ribbon strands cover the selected carrier from its surface normals without detached clumps.
- **Original fur-volume replacement is not supported.** The inferred carrier follows the outer malformed coat volume rather than an authenticated underlying skin. Insetting and silhouette-matched strand calibration remain untested.

Open `sheet.html` for the complete source, sweep, visual selection, and groom comparison. `campaign.json`, `result.json`, `carrier-selection.json`, `final-result.json`, and `visual-admission.json` preserve the evidence and claim boundaries.
