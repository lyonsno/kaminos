# FLUX Surface Matrix

This matrix edits the exact promoted frontal creature source at `../flux-81408/output.png`. It tests six coat-representation interventions and four broader stylization basins. Every prompt is fired with matched route settings across wave-one seeds `81410` and `81411`, then wave-two seeds `81412` and `81413`.

The source remains the sole conditioning image. Camera, framing, projection, pose, and anatomy are carried by its pixels rather than requested in text. The prompts are preserved verbatim under `prompts/`; `matrix-plan.json` records the source digest, route request, seed structure, comparison contract, and claim ceiling.

Greenroom completion is route evidence only. Promotion requires direct visual comparison for facial identity, horn integration, complete framing, broad surface organization, and absence of newly introduced semantic damage. TRELLIS improvement remains unclaimed until a selected edit is reconstructed and inspected in orbit.

All 40 cells completed and were directly inspected. `matrix-review.md` records the four-seed ranking and the exact two-source TRELLIS continuation. `04-faceted-fur-planes` is the stable identity/surface winner; `08-feature-animation` is the stable clean-shape comparator.
