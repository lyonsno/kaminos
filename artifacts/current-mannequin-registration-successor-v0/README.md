# Current mannequin registration successor v0

This assay asks whether the improved authored cat envelope stays structurally legible under three concise elaboration prompt classes, then whether selected results reconstruct into Trellis casts that register to the operator-authored skeleton.

The FLUX screen crosses `mannequin`, `armature`, and `fur` prompts with seeds `80301` and `80413`. All six cells use the authenticated full-density source plate and the same FLUX.2 Klein route. After visual inspection, no more than three happy-safe, silhouette-preserving, materially distinct results advance to Trellis.

Trellis success is evaluated from six orbit views after one global similarity fit: uniform scale, rotation, and translation only. Registration is inspected at the skull, shoulder, spine, pelvis, limb chains, contacts, and tail root. No local deformation or proportion editing is allowed before that judgment.

Run `python3 submit_flux.py` to submit only missing cells. The script writes `submission-ledger.json` immediately so queue identity remains attached to every cell.

After collection and registration, run `python3 build_campaign_screen.py`. The resulting `campaign-screen.html` is the operator-facing evidence surface: exact source, prompt, settings, FLUX result, Trellis cast, and registration witness remain adjacent. `RESULT.md` records the campaign-level interpretation and claim ceiling.
