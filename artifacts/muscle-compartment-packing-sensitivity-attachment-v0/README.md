# K8 attachment sensitivity: visual-first formation refusal

This artifact is a diagnostic assay, not an admitted packing result. It binds the numerical result for every attachment-scale variant to an independently captured, agent-inspected before/output pair. The whole portfolio is `visually-rejected`.

## Current comparison identity

- Source file SHA-256: `0577bc78607bc418b5cee376f09a7fb5b23018be3b3de04e9bfab16749e9d2b3`
- Effective source identity: `synthetic-muscle-density-ladder-8-v0@ae2fd71cf78af2430cd382ca6fcd2e78164b9193684a1a7aa83864f740c95381`
- Formation reference: `centerlineSmoothingReference=source-displacement`
- Request: `synthetic-k8-attachment-bracket-visual-v0`
- Run: `run-9b1bd4443c00a7dcbf24`
- Requested/effective assay route: `muscle-compartment-packing-sensitivity-orbitable-v0`; no fallback
- Requested/effective capture route: `independent-headless-screenshot-v0`; no fallback
- Visual inspection receipt: `visual-inspection.json`

Each admitted capture URL matches its current variant artifact root and state, the current source/packed/HTML bytes match the assay report, browser stderr is untruncated and contains no uncaught exception, the capture process group is gone, and its temporary profile is removed. Repeating the same visual-admission command leaves the report, receipt, and portfolio byte-identical.

The source and request were read-only inputs. Their hashes were checked before and after the solve, and the report records `mutated=false` for the source.

## Result

| Variant | Solver disposition | Iterations | Pairwise residual | Skeletal residual | Volume error | Source/packed maximum bend energy | Minimum source tangent cosine | Visual disposition |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `attachment-0500` | `continuous-clearance-failed` | 480 | `4.77657435e-4` | `0` | `0` | `0.2592 / 3.201988270714` | `-1` | `visually-rejected` |
| `attachment-0545` | `continuous-clearance-failed` | 1280 | `8.48528e-7` | `0` | `0` | `0.32805 / 2.046090935563` | `-1` | `visually-rejected` |
| `attachment-0550` | `source-formation-failed` | 804 | `9.8812e-8` | `0` | `0` | `0.3362 / 1.959671905082` | `-1` | `visually-rejected` |

All three outputs clear the skeletal obstacle and approximately remove pairwise penetration, but they straighten the bowed/crossing source carriers into a generic radial cylinder array. Every carrier records a longitudinal tangent reversal. The `attachment-0550` solve reaches the clearance tolerance, but its near-zero curvature cosine, inflated bend energy, and tangent reversal make `source-formation-failed` the correct solver disposition.

The visible falsifier is stronger than the old scalar result: collision removal alone does not preserve authored formation. The next solver mechanism must preserve curvature magnitude and longitudinal order while negotiating clearance; expanding this attachment scale sweep would only repeat the same failure mechanism.

## Superseded intermediate episodes

- `run-269e262ab66cebab4174` consumed an older source identity, `f4e5207394a214574862aaea9ea37f17429d2bf96d8c9a355c73e08147464431`, which omitted the formation reference. Its 33-iteration `attachment-0550` convergence is stale-comparison anti-evidence and is not a replication of the current-source assay.
- `run-31918cdc369da434192e` consumed the current source and produced the same visible radial-cylinder failure, but predated the source-formation admission classification. Its visual finding is preserved here; `run-9b1bd4443c00a7dcbf24` is the authoritative current-source, formation-aware episode.

The superseded generated run directories are intentionally not part of this artifact. Their decision-bearing provenance and result distinction are preserved above; the current run contains the inspectable images and machine-readable reports.
