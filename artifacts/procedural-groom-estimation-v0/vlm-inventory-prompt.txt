You are observing three unlabeled, membership-neutral renders of the same procedural creature-head carrier from different viewpoints.

Inventory every visibly distinct fibrous system you can justify from the images. Do not assume that supplied categories exist, and do not merge regions merely because they share color. For each proposed system, return:

- a short locally descriptive id;
- which views support it;
- one concise literal phrase suitable for prompting an image segmenter;
- a normalized bounding box [x_min, y_min, x_max, y_max] for every supporting view;
- dominant 2D flow direction [x, y] for every supporting view;
- relative length, density, outward puff, and confidence in [0, 1];
- the visual evidence that makes it distinct from its nearest neighboring system.

Separately inspect for whiskers. If whiskers are present, do not propose segmenting individual strands. Locate the left and right muzzle pads from which they emerge, provide one literal segmenter phrase for each pad, and estimate whisker presence probability, length relative to muzzle width, sparseness, fan angle, sag, and confidence. If they are absent or ambiguous, say so explicitly.

Return JSON only. Preserve uncertainty. Do not invent hidden 3D structure, anatomy, material identity, or production suitability.
