# Provisional Cat Envelope Representation Assay

This fixed-route seed-80301 comparison separates two effects that the earlier palette assay left confounded.

The source-derived low-frequency envelope retains substantial authored morphology: the folded rear support, low front, long limb chains, and major mass relationships survive much more strongly than they do in the source-ablated control. The envelope therefore buys a useful morphology channel even though this first compiler is provisional.

All three outputs nevertheless become exposed-anatomy cats. The neutral plate makes the cause legible: the frozen prompt itself produces a generic feline ecorche. Its references to operator-authored anatomy, muscle trajectories, joint chains, and biological completion outweigh the later request for no exposed anatomy. The current result does not falsify analytical skin; it falsifies this prompt as a clean skin-completion test.

The source-fixed prompt ablation is strongly positive. Replacing anatomy-led language with a concise exterior-only prompt turns the same envelope into a continuous fur-covered cat. Relative to the blank control, the conditioned output retains a distinct rear-heavy mass, crouch, front reach, and tail event. The prompt therefore controls surface class while the source still controls substantial low-frequency morphology.

The remaining failure is directional registration. The right-facing source becomes a left-facing output under the exterior prompt. The next informative assay is a second registered view or an explicit source-space orientation cue, with the envelope, prompt, seed, and generation route otherwise frozen.

Claim ceiling: these exact fixed-route comparisons establish route-local morphology preservation, prompt-dependent surface-class control, and one polarity failure. They do not establish behavior across other seeds, views, compilers, or generators.
